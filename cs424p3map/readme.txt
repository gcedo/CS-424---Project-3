A semi-auto squareStatesMap configuration tool

MOUSE:
drag squares to move individual state

KEYBOARD:
`aswd` to move 1 pixel at a time
`ASWD` to move 10 pixel at a time
`z` to make it smaller
`w` to make it larger

`p` to save change

IT'S NOT FINISHED
